#include <stdio.h>
typedef struct{
char company[20];
char name[10];
}Owner;

typedef struct{
int year;
char Brand[10];
char name1[15];
Owner owner;
}car;

int main(){
Owner owned={"Tata-motors","JLR"};
car s={1970,"SUV","Range-Rover",owned};
printf("Introduced in year:%d brand %s (%s),Owned by:%s by %s\n",s.year,s.Brand,s.name1,s.owner.company,s.owner.name);
return 0;
}
